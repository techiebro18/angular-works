/**
 *
 * `type.extensions.ts` is the entry point and aggregator file of other extension files.
 *
 * Please import below any other extension file you want to add into the application.
 *
 */

import './string.extensions';
import './array.extensions';
